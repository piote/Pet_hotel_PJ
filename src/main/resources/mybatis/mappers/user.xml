<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.user">

   <resultMap id="userResult" type="UserVO">
      <result property="user_id" column="USER_id" />
      <result property="user_pwd" column="user_pw" />
      <result property="user_name" column="user_name" />
      <result property="user_email" column="user_email" />
      <result property="user_tel" column="user_tel" />
      <result property="user_tel_sub" column="user_tel_sub" />
      <result property="user_message" column="user_message" />
      <result property="user_birth" column="user_birth" />
      <result property="joinDate" column="joinDate" />
      <result property="user_grade" column="user_grade" />
   </resultMap> 

   <select id="selectAllUserList" resultMap="userResult">
      <![CDATA[
        select * from TB_USER order by joinDate desc			
      ]]>
   </select>
   
  
	<select id="login" resultType="userVO"  parameterType="java.util.Map"  >
	    <![CDATA[
			select * from TB_USER
			 where member_id=#{user_id}
			 and member_pw=#{user_pw}			
			 
		]]>
	</select>
 
</mapper>